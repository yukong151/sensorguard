# 2026-08-12

## SensorGuard T2 链路全量代码审查
- 通读两份开发文档 + Kotlin 探针/服务/UI + Rust FFI/事件窗/规则引擎 + fbs schema。
- **核心结论**：T2 是"活着的死分支"。解析器(`SensorServiceParser`)对 Moto XT2153_1/Android13 实测格式健壮，但：
  - P0-1: Shizuku 只 `addSharedActivePair`，从不推 `OpEvent`；Rust `sg_tick` 仅对 `pw.changed()`(需 OpEvent 置位)的窗口判定 → IMU 对永不被判定。
  - P0-2: `samplingPeriodUs` 在 `GuardService` 回调被丢弃；`ActivePair`/`TickInput` 无 rate 字段；引擎 `sample_rate_hz` 由事件间隔反推，语义错。
  - P1: 授权晚于启动则不激活(无 permission listener)、`execDumpsys` 无超时单线程可挂死、Shizuku 停止客户端只增不删。
  - P2: `DetailActivity` 把 T2 误标 T0；`build.gradle` 硬依赖 shizuku api 却又反射(违背<4MB 目标)。
- 推荐打磨顺序：Step1 打通归因(推 START/STOP OpEvent+diff 删除) → Step2 贯通 rate(fbs 增 `sampling_period_us` 并覆盖 eval_ctx.sample_rate_hz) → Step3 可靠性(listener/超时/清理) → Step4 UI/体积。
- 真机前提已满足：Moto XT2153 + Shizuku 已装。报告见 `F:\KTV\docs\T2代码审查与打磨清单.md`。
- 下一步待用户指派：实现 Step1+Step2（T2 本体）。

## T2 Step1+Step2 已实现（用户按推荐进入开发）
- 代码落地：Shizuku 轮询结果做 diff → 推 START(新增)/STOP(消失)/TICK(持续) OpEvent，均携带 `samplingPeriodUs`；修复 IMU 对永不被判定(P0-1)与停止客户端只增不删。
- fbs `OpEvent` 增 `sampling_period_us:long`(slot6)，`build.rs`+flatc 重生成 `sensorguard_generated.rs`；Rust `PairWindow` 存物理采样率、`sample_rate_hz()` 优先物理值(修复 P0-2)。
- Kotlin 契约同步：`ProbeEvent`/`OpEventData` 加 `samplingPeriodUs:Long=0`、`FbSerde.encodeOpEvent` startTable(7)+slot6、`GuardService` 透传并加 `persist` 参数(TICK 不落加密库)、`FbSerdeTest` golden 更新为 80B 新字节。
- `ShizukuProbe` 加 `isRunning` 守卫；`GuardService` 未授权时 5s/15s 轻量重试探活启动。
- 验证：Rust `cargo test` 68 全绿；golden 重生成并逐字符比对一致。**Kotlin 因本机缺 Android SDK 未编译**，需用户侧 assembleDebug 后真机回归(Moto XT2153+Shizuku 授权)。
- 实现记录：`F:\KTV\docs\T2实现记录_Step1+2.md`。Step3(listener/超时/清理) 与 Step4(UI/体积) 未做。

## T2 Step3 已实现（用户回复"开始"）
- `ShizukuProbe.kt` 重写：`isAvailable` 改为"binder 存活 **且** 已授权"(修"装了未授权误报 T2");反射注册 `BinderReceived/BinderDead/RequestPermissionResult` 三类监听器实现晚授权/服务重启自动激活停用;`execDumpsys` 加 15s 超时(独立 ioExecutor 线程,避免卡死 scheduler 单线程)。
- `GuardService.kt` 探针创建简化为 `ShizukuProbe{...}.also{ it.start() }`,删 5s/15s 手工重试(start() 内部已含监听器+晚激活)。
- `DetailActivity.kt` 补 `tierName()`: T2(Shizuku)/T1/T0(原只渲染 T1/T0 漏 T2)。
- `build.gradle.kts` 加 Shizuku 硬依赖取舍注释(dynamic-feature 拆分留 Step4 决策)。
- 验证状态同 Step1+2:Kotlin 未编译(缺 Android SDK),Rust 侧无回归。**待用户 assembleDebug 真机回归**(晚授权/服务死亡/超时/T2 展示四路径见 `docs/T2实现记录_Step3.md`)。
- 实现记录：`F:\KTV\docs\T2实现记录_Step3.md`。建议下一步:真机全链路回归确认后转交付,或做 Step4 dynamic-feature 拆分。

## 真机回归环境 & 工具链事实（重要，复用）
- 设备在线：`adb -s ZY22DDK2FL`（Moto XT2153_1，XT2153）已 `device`；ADB=`C:\Users\ew\AppData\Local\Android\Sdk/platform-tools/adb.exe`。
- SDK=`C:\Users\ew\AppData\Local\Android\Sdk`；NDK=`...\ndk\28.2.13676358`；cargo-ndk=`C:\Users\ew\.cargo\bin\cargo-ndk`；JDK17=`C:\Users\ew\jdk17`（gradlew 需 `JAVA_HOME` 指向它）。
- **路径坑**：cargo-ndk 是原生 Windows 程序，不接受 `/c/...` 路径，必须用 `cygpath -w` 转 Windows 风格（ANDROID_HOME/ANDROID_NDK_HOME/输出目录）。
- `./gradlew assembleDebug` 首次需经 wrapper 下载 gradle-8.7-bin，**当前网络仅官方可达且 ~30KB/s（约 60 分钟）**；腾讯/清华/华为/阿里镜像均返回 0 字节，curl 到官方仅 169B。本机 ~/.gradle/wrapper/dists 有 8.11.1/8.13/9.1.0 但均未解包，无法借用。
- 已验证：Rust arm64 `.so` 用 `cargo ndk -t arm64-v8a -o <WIN路径/jniLibs> build --release` 可成功（注意 -o 给 jniLibs 根目录，cargo-ndk 会自动加 `arm64-v8a/` 子层，勿多给一层否则嵌套）。
- 真机 `dumpsys sensorservice` 实时验证：`0x0100000b`(accel,200000us)/`0x01000052`(prox) 等 IMU 连接真实存在，格式与 `SensorServiceParser` 正则完全匹配 → T2 归因链路在真机数据上成立。原始 dump 存 `F:\KTV\docs\dumpsys_sensorservice_live_XT2153.txt`。
- 状态：`.so` 已重编(23:13)、Kotlin 改动待 assembleDebug 完成；构建在后台跑，完成后装真机做四路径回归（晚授权/服务死亡/超时/T2 展示）。

## 关机前保存（23:48，电脑重启）
- **目标**：用户中途要求保存工作。已写专用恢复指南 `F:\KTV\docs\T2回归_恢复指南.md`（含环境事实/卡点根因/重启步骤/四路径清单），本段为精简摘要。
- **进度**：Step1+2+3 代码改动 + Rust `.so` + 真机 dumpsys 校验 全部完成落盘；唯一阻塞 = `./gradlew assembleDebug` 须先拿到 Gradle 8.7。
- **卡点已排除项（勿重复试）**：①本机 ~/.gradle 下 8.7/8.11.1/8.13/9.1.0 的 dists 目录**全是空壳**（仅 docs/，无 bin/gradle、无 zip），无法借用；②腾讯云/华为云 gradle 镜像返回 200/206 但 **0 字节**（完整 GET 与 Range 都 0），清华/Aliyun 404；③官方 307→github 302→release-assets(Azure CDN,带 60min SAS) 走 wrapper 的 Java 下载器 ~17KB/s，curl 单连接偶达 76KB/s 但不稳（常 0 字节）；④GitHub 代理(ghproxy/moeyy 等)关机前最后测试**未返回数据**，重启后可再试。
- **关键修正**：后台 Bash 与 `/f/KTV` **共享**（后台能读写前台文件，已用 L06LDk 验证）；此前误判隔离实为重定向目录不存在使脚本未执行。→ 长下载可放后台、前台 `ls` 轮询。
- **JDK PATH 坑（已确认）**：Git-Bash 下 `PATH` 须用 **Unix风格** `/c/Users/ew/jdk17`，反斜杠 `C:\...\jdk17\bin` 会让 `java` 找不到（gradlew 自身能探测 java，但 shell 里 `java -version` 会失败）。
- **AGP 8.5.2 → Gradle ≥8.6**，故 wrapper 指定的 8.7 合适；8.11.1/8.13/9.1.0 也兼容但同样未解包。
- **清理**：已删 gradle-8.7 `.part`/`.lck` 与 `/f/KTV/.gradle_dl` 探针残留，wrapper 重启后干净重新下载。后台任务随关机终止。
- **重启后第一步**：设 `JAVA_HOME=/c/Users/ew/jdk17` + `PATH=/c/Users/ew/jdk17/bin:$PATH` + `ANDROID_HOME` → `./gradlew assembleDebug`（必要时把 distributionUrl 换 GitHub 代理加速）→ 出 `app-debug.apk` → `adb -s ZY22DDK2FL install -r` → 四路径回归（见恢复指南）。

## 事件时间线「传感器名+归因」UI 落地 + 滥用判定（本轮）
- **需求**：时间线看不出调用的是哪个传感器；开发阶段可显示包（规避大厂法务，当前阶段不用管），但上架市场绝不向用户明示身份；判定 22 条告警是否真滥用。
- **代码改动（已落盘、静态复查通过）**：
  - `ProbeModels.kt`：`ProbeEvent` 增 `sensorName`（仅 UI 展示，不落加密库）。
  - `SensorServiceParser.kt`：新增 `handleToName` 映射 + `nameOf()`/`NAME_RE`，从 Sensor List 段取 `lsm6dso Accelerometer` 等显示名；`SensorClient` 增 `sensorName`。
  - `GuardService.kt`：增 `pkgHashInfo`（包指纹→(内层包名,uid)）供 UI 归因；`pushProbeEvent` 填充、`attributionFor(hex)` 暴露；`pushShizukuEvent` 透传 `sensorName`。
  - `AppAttribution.kt`（新建）：`resolve(ctx, innerPkg, uid)` —— **DEBUG 构建**返回"宿主 App(包) › SDK:内层包"，Release 返回 null（UI 以"某应用"替代，合规门控）。
  - `item_timeline.xml` + `TimelineActivity.kt`：两行列表项，事件行=`[OP] 传感器名 · 相位`/副=`归属 · 时间`；告警行带归因+调试版附包指纹前 8 位；标题栏 DEBUG 加"（调试版·含 App 归属）"。
  - `DetailActivity.kt`：`formatVerdict` 末尾增 DEBUG 归属行。
  - 删 `AppAttribution.kt` 未用 import（防 allWarningsAsErrors）。
- **构建结论（关键，勿重复试）**：agent 环境**无法** assembleDebug。根因三重：①跨用户：Bash 进程非 `ew`，写 `C:\Users\ew\.gradle`（含 wrapper `.lck`）被拒；②Gradle 8.7 原生 `native-platform.dll` 在本代理运行时**加载被拦**（dll 141KB/64位、MSVC 运行时齐全、ACL 已 Everyone 全控仍失败）；③`cmd.exe` 被 Bash 安全策略禁止调用。→ **只能用户本机 PowerShell 跑 `tools\build_apk.ps1`**（脚本已设对 JAVA_HOME/ANDROID_HOME/GRADLE_USER_HOME=C:\Users\ew\.gradle）。
- **滥用判定（答用户第 4 问）**：22 条绝大多数是同一条 `kind=OBSERVE / cat=SIDE_CHANNEL / sev=40 / rule=112`，仅在 ACCEL/GYRO；该规则**对 Android 系统自身也误报**（`uid=1000` FaceDownDetector、GMS `uid=10213`）。→ 多为"该应用在用运动传感器"提示而非确认滥用。确属滥用集中在：知乎/优酷摇一摇开屏广告 SDK（`com.zhihu...shake`/`com.youku.xadsdk...SplashShakeView`）、Smart Video Downloader 三家广告 SDK（Pangle+AN+AppLovin）同听 IMU。建议引擎加系统 uid 白名单 + 更精准特征（后台高频采样/摇一摇 SDK 包名模式/采样率异常/结合前后景）。详见 `docs/sensor_client_attribution_report.md` 第八节。
- 临时 `F:\KTV\.gradle_home` / `.gradle_agent` 已清，工作区无污染。
